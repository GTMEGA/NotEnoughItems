package codechicken.nei.api;

/**
 * Implement this interface on a GuiContainer to enable the autofocus search in NEI (if the autofocus option is enabled)
 * without needing to add the classname to the config file .
 */
public interface INEIAutoFocusSearchEnable {
}
